**************************************
		PREMIA
**************************************


The program directory the following files:

- source files : main.c, function_to_minimise.c, BFGS_InteriorPoints.c, capfloor_black.c, capfloor_hw2d.c, swaption_black.c, swaption_hw2d.c, read_market_data.c, InitialYieldCurve.c and their correspondant header files.

- a binary executable file "Calibration_HullWhite2d" generated by the compilation

- an example of input market data:  brigo_mercurio_zc.dat, brigo_mercurio_cap.dat, brigo_mercurio_swaption.dat

- make files : Makefile Makeuser.incl


Compilation 
===========

Compilation command under Linux: use the command make
This generates the executable binary file "Calibration_HullWhite2d". 


Execution
=========

Type ./Calibration_HullWhite2d to run the calibration program. 

You can choose either to calibrate on cap or swaptions volatilities.

Also five parameters can be calibrated in this program and it's possible to 
- choose the boundaries for these parameters or use default values. 
- choose the initial point for optimization or use a random point in the boundaries set.


Output of results
=================

The user can choose to print the value of parameters and error function at every step of the optimization algorithm.

At the end, we print the output parameters, the error function value and the volatility table.

Market Data
===========

The market data a stored in three files:

brigo_mercurio_zc.dat : contains zero coupon curve
brigo_mercurio_cap.dat : contains the atm-the-money cap volatility quotes
brigo_mercurio_swaption.dat : contains the atm-the-money swaption volatility quotes
**************************************
		PREMIA
**************************************


The program directory the following files:

- source files : main.c, function_to_minimise.c, BFGS_InteriorPoints.c, capfloor_black.c, capfloor_hw2d.c, swaption_black.c, swaption_hw2d.c, read_market_data.c, InitialYieldCurve.c and their correspondant header files.

 - a binary executable file "Calibration_HullWhite2d" generated by the compilation

 - an example of input market data:  brigo_mercurio_zc.dat, brigo_mercurio_cap.dat, brigo_mercurio_swaption.dat


Compilation 
===========
Compilation command under Linux: use the command make
This generates the executable binary file "Calibration_HullWhite2d". 

IMPORTANT : To compile this project, the library pnl should be already compiled.

Execution
=========

Type ./Calibration_HullWhite2d to run the calibration program. 

You can choose either to calibrate on cap or swaptions volatilities.

Also five parameters can be calibrated in this program and it's possible to 
- choose the boundaries for these parameters or use default values. 
- choose the initial point for optimization or use a random point in the boundaries set.


Output of results
=================

The user can choose to print the value of parameters and error function at every step of the optimization algorithm.

At the end, we print the output parameters, the error function value and the volatility table.

Market Data
===========

The market data a stored in three files:

brigo_mercurio_zc.dat : contains zero coupon curve
brigo_mercurio_cap.dat : contains the atm-the-money cap volatility quotes
brigo_mercurio_swaption.dat : contains the atm-the-money swaption volatility quotes

